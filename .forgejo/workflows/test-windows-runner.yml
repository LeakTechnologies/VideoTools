name: Windows Runner Test

on:
  workflow_dispatch:

jobs:
  test-windows-runner:
    runs-on: windows
    steps:
      - name: Test echo
        shell: bash
        run: |
          echo "Windows runner is working!"
          echo "Computer: $HOSTNAME"
