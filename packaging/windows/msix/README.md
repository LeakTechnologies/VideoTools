# Windows MSIX Packaging

This folder holds Windows MSIX packaging artifacts.

Planned contents:
- `VideoTools.msix` (signed installer)
- `AppxManifest.xml` (generated)
- Signing certificate and notes (if required)

Build notes will be added once the MSIX pipeline is wired up.
